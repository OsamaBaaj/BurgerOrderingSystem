# Burger Ordering System

A simple desktop application built using C# and Windows Forms.

The application allows users to customize a burger order by selecting toppings, cooking level, order type, and quantity while dynamically updating the total price and order summary.
